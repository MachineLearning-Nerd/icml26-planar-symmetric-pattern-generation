# Method

Evaluate independently constructed coefficient and basis functions before and
after the target group actions for every paper-listed planar group. Report the
maximum absolute invariance residual. Deliberately non-invariant coefficients
must violate the threshold.
