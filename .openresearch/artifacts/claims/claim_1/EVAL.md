# Claim 1 evaluator note

Verdict: **VERIFIED**.

Run `uv run --frozen python reproduce.py`. The verifier exits nonzero if the
17-group ledger, subgroup test, conjugation recovery, or negative control
fails. See `raw_results.json`, `checker_output.json`, and
`negative_control_output.json`.
