# Claim 2 source audit

Main-text Theorem 3.3 (`#S3.Thmtheorem3`) uses the quantifier order

1. fixed \(r=[W_a:G]\) and fixed \(G\)-invariant \(\eta_i\);
2. every \(f\in C_G(\mathbb{R}^2)\);
3. every \(\epsilon>0\);
4. some \(h_i\in C_{W_a}(\mathbb{R}^2)\);
5. unit-cell **L1** error below \(\epsilon\).

Appendix Theorem C.12 states an L2-integral version. This is not used as a
silent substitution: the reconstructed proof directly obtains uniform error,
which bounds the main theorem’s L1 integral. The derivation fills in the
appendix’s compressed “module generated by \(g_i\) is dense” sentence by first
Reynolds-projecting into \(P_{\mathrm{torus}}^G\) and then applying the exact
Hironaka free-module decomposition.

The relevant assumptions are compactness/finite measure of the periodic cell,
uniform density of trigonometric polynomials, finite Reynolds averaging, and
the rank-\([W_a:G]\) Hironaka module result.
