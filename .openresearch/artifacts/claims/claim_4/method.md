# Method

Generate deterministic scalar fields with the algorithmic group projection and
evaluate them under independent group actions. The no-projection control must
exceed the same threshold by at least nine orders of magnitude.
